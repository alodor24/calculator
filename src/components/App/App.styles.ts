import { Box, styled } from '@mui/material';

export const Wrapper = styled(Box)`
  &.MuiBox-root {
    box-shadow: 0 1rem 3.3rem -.5rem rgba(96,125,139,0.39);
    background-color: transparent;
  }
`;
