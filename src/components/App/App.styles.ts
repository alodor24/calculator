import { Box, styled } from '@mui/material';

export const Wrapper = styled(Box)`
  &.MuiBox-root {
    box-shadow: 0 1rem 3.3rem -.5rem rgba(96,125,139,.7);
    background-color: transparent;

    border-radius: 0 0 0.4rem 0.4rem;
    overflow: hidden;
  }
`;
